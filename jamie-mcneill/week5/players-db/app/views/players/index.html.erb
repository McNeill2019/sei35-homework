<h2>Current Rankings:</h2>

<h5>Click on a player for player stats:</h5>

<ol>
   <% @players.each do |player| %>
      <li>

        <a href="<%= player_path(player.id) %>">
          <%= player.name %>
        </a>
        <img class ="thumb" src="<%= player.image %>" alt="Player Image">

      </li>
   <% end  %>
</ol>
